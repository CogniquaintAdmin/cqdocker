name: Build and Push ERP Image V16

on:
  push:
    # Trigger on version tags
    tags:
      - 'v*'
    # Trigger on direct pushes to the development branch
    branches:
      - version-16

jobs:
  build:
    runs-on: ubuntu-latest

    env:
      GIT_REPO_PATH: https://github.com/frappe/frappe
      GIT_BRANCH: version-16
      PYTHON_VERSION: "3.11.6"
      NODE_VERSION: "24.14.0"

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Extract version
        id: vars
        run: |
          # If triggered by a tag, use the tag name. If triggered by a branch, use 'latest'
          if [[ $GITHUB_REF == refs/tags/* ]]; then
            echo "VERSION=${GITHUB_REF#refs/tags/v}" >> $GITHUB_OUTPUT
          else
            echo "VERSION=latest" >> $GITHUB_OUTPUT
          fi

      - name: Install jq
        run: sudo apt-get update && sudo apt-get install -y jq

      - name: Encode apps.json to base64
        id: apps
        run: |
          APPS_JSON_BASE64=$(cat apps.json | jq -c . | base64 -w0)
          echo "APPS_JSON_BASE64=$APPS_JSON_BASE64" >> $GITHUB_ENV

      - name: Login to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_PASSWORD }}

      - name: Set up QEMU
        uses: docker/setup-qemu-action@v3

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Build and Push ERP Image
        uses: docker/build-push-action@v5
        with:
          context: .
          file: ./dockerfile
          push: true
          build-args: |
            GIT_REPO_PATH=${{ env.GIT_REPO_PATH }}
            GIT_BRANCH=${{ env.GIT_BRANCH }}
            PYTHON_VERSION=${{ env.PYTHON_VERSION }}
            NODE_VERSION=${{ env.NODE_VERSION }}
            APPS_JSON_BASE64=${{ env.APPS_JSON_BASE64 }}
          # Securely pass the PAT for the Dockerfile --mount=type=secret
          secrets: |
            "GH_PAT=${{ secrets.GH_PAT2 }}"
          tags: |
            cogniquaint/test16:${{ steps.vars.outputs.VERSION }}
            cogniquaint/test16:latest